{
  "name": "stencil-demo",
  "license": "MIT",
  "version": "0.0.1",
  "scripts": {
    "docs": "stencil build --docs-readme",
    "build": "stencil build --prerender",
    "deploy": "gh-pages -d www -m 'deploy'",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec",
    "test:e2e": "stencil test --e2e"
  },
  "dependencies": {
    "@stencil/core": "^1.0.2",
    "@stencil/router": "^1.0.1"
  },
  "devDependencies": {
    "@types/jest": "24.0.13",
    "gh-pages": "^2.0.1",
    "husky": "^2.4.0",
    "jest": "24.8.0",
    "jest-cli": "24.8.0",
    "workbox-build": "4.3.1"
  }
}
